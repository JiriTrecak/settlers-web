# map

Hardcoded match map. `MAP_ID` is the only playable id. `MAP_SIZE` is cells on each axis (square, 1 world unit per cell).

`utcmap.ts` is the authored file: `.utcmap` JSON. `name` is the document title. `stamps` are placed catalog assets.
