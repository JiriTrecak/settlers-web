# building

Built sprites from `buildings/{civ}/{kind}` variant `built`; plans use `scaffold` (falls back to built). One sprite per hut at the origin tile, DAT offsets. Texture swaps when the hut finishes. z = `isoDepth` on the shared iso container.
