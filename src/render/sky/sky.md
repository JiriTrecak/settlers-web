# sky

24h key light. `Sky` owns ambient, hemisphere fill, and the shadow-casting sun. `hour` is 0–24; `playing` advances it. Renderer ticks it every frame. Editor Light dock scrubs and runs the cycle. Unlit leaf cards get `paint` multiplied onto Unity tint so canopies grade with the same clock.
