# load

`LoadStatus` is the match-start overlay. Session pushes `LoadView` (`stage`, `detail`, `done / total` textures, last path, elapsed). The widget coalesces paints to animation frames. Destroy it when `start()` finishes.
