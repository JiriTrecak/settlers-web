# bar

`IconBar` — Lucide icon dock with Outfit captions. `place: "top" | "left" | "inline" | "col"`. `surface: "plain"` when a parent already draws the sheet. An action may list `modes` — chrome shows that as a sibling vertical when the tool is on.
