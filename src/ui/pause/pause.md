# pause

F10 modal in the overlay. Session constructs it and feeds `PauseView` + the save list for this mode (SP files in SP, MP files in MP). Clicks go back out as hooks. SP pause is Session's job, not this widget's.
