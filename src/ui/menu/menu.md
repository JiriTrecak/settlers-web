# menu

`GameScreen`s. Own a single `root`. `ScreenHost` swaps them.

- `menu.ts` — Single player / Multiplayer / Asset browser
- `mapSelect.ts` — player tint swatches + grouped map list → `onPick(id, player)`
- `notice.ts` — stub (multiplayer)
