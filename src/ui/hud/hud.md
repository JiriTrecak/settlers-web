# hud

In-match overlay: coords, help, Menu button. Styles in `styles.css` (also menu chrome, minimap, speed).

Leave: Menu click or Escape (via `MapInput`). Minimap sits to the right of Menu; speed under the minimap.
