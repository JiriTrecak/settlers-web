# build

HTML strip under the speed control. **Buildings** / **Units** tabs. Huts: lumberjack / forester / stonecutter / sawmill / house / tower; click again to deselect. Session places a `plan` on empty valid owned land and drops the tool. Ghost is red if illegal or the lumberjack plot is not yet level (click still places). Units is a debug dump: T1 swordsman 1 / ×10 / ×100, sticky (click again to drop). No barracks, no cost. Lookout / big tower are not on the strip.
