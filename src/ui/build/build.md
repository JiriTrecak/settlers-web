# build

HTML strip under the speed control. Pick lumberjack / forester / stonecutter / sawmill / house / tower; click again to deselect. Session places a `plan` on empty valid owned land and drops the tool. A finished tower stamps another occupy disk. No flatten preview. Lookout / big tower are not on the strip.
