# build

HTML strip under the speed control. Pick lumberjack / forester / stonecutter / sawmill / house / tower; click again to deselect. Session places a `plan` on empty valid owned land and drops the tool. Ghost is red if illegal or the lumberjack plot is not yet level (click still places). A finished tower stamps another occupy disk. Lookout / big tower are not on the strip.
