# object

`ObjectGrid`: at most one tree, stone, or stack per tile. `blocks()` is walkability. Chop writes `stateProgress` then replaces the tree with a trunk stack. `trunkStack` is the shared place payload.
