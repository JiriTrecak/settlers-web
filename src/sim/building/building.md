# building

`BuildingGrid`: huts with a multi-tile footprint from the def. `blocks()` is walkability (blocked tiles). `protects()` is "no second hut here" (blocked + skirt). `plan` is a scaffold waiting for `constructionStacks`; `built` is finished. Flatten / bricklayers still skipped.
