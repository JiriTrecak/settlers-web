# chrome

Editor docks. One top sheet (name + file), tools left. Grid toggles a sibling mode dock (Tiles / Full / None). Brush opens a sibling dock: asset squares + % / scale (same asset can appear twice), New empties the kit, named presets, size / density / Apply. Gamecam latches the play camera. Asset chip bottom-left. Camera binds bottom-right. Catalogue is a modal with iso mesh snapshots.
