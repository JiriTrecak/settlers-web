Soft weight mask the foliage brush paints. `BrushKit` is the weighted asset set — slots have their own id, so the same mesh can sit twice at different scales. Apply runs `scatterBrush` into `MapStamp`s. Water-type slots only land on wet cells. Named kits live in `BrushPresetStore` (localStorage).
