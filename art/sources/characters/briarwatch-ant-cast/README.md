# Briarwatch ant cast

See the [art handoff](../../../concepts/briarwatch/README.md) for source, geometry, runtime, validation and limitations.

Build from `model.py` through the studio. The packed generated reference and `samples.json` define the palette; `.blend` and exported GLBs are retained in this directory.
