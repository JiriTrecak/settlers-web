# P2 — next systems

Not more buildings. Not water. Not sound. Those don't change the machine.

Four pillars: **controllable units**, **flatten**, **N players in one match**, **lockstep-shaped sim**. Everything below is in service of those. AI can be a script. Netcode is last.

Do them in this order. Later steps assume earlier ones exist.

---

## 1. Action queue + checksum

Landed. `enqueue` default is next beat; `dispatch` is now; `placeColony` is an action; `checksum` / `log` / `replay` exist. See `tests/engine/queue.test.ts`.

---

## 2. Per-player matcher

Landed. Requests/offers are player-keyed. Hut offer tiles belong to that hut; free-standing piles use `land.owns`. Construction recruit is the hut's player too. See `tests/engine/economy.test.ts`.

---

## 3. Selection + pioneer

Landed. Click pioneer; RMB works (pioneer claims one unenforced tile / 1.2s, search 6 then 30). Shift-drag marquees a group. F3 claim stays the tower-disk cheat. Kit has no pioneers — opponent script converts a bearer. Distinct pioneer clips (`settlers/roman/pioneer`). See `tests/engine/pioneer.test.ts`.

---

## 4. Flatten

Landed. Every hut flattens unless `flatten: false`. Target = integer mean of protected, frozen on the plan. Diggers kneel 1s, ±1, paint flattened. `ceil(n/15)`. Already-level skips. Ghost red on a slope, click still places; mark > 127 refuses. Mesh samples `hiddenAt` in grey; `terrainGen` patches the dirty cells. See `tests/engine/flatten.test.ts`.

---

## 5. Second player, dumb script

Landed. Match stamps a colony at `starts[i]` (2 when the dump has 2+ starts). Local view is `world.view(me)`. First occupying hut of a player may stamp unowned land so the second HQ lands; extra towers still need owned. Session `Opponent` thinks every ~5s: pioneer toward the human, lumberjack → sawmill → stonecutter (wait until built), then extra towers from the closest T1. Overlapping occupy is the existing don't-steal rule. Defeat stub waits. See `tests/engine/players.test.ts`.

---

## 6. Combat / soldiers

Landed. L1 swordsman: `needsPlayersGround: false`, 338 ms step, 100 hp, 10 dmg, 1s swing, auto-aggro hex 30. RMB `moveTo` (shift = forced: ignore fights until the path ends). DEFAULT walk peels into an `attack` job. Death splices the unit and unstamps fog (no ghost sprite). **X** enlists an empty bearer — barracks later. Bearers / workers are not combat targets. T1 garrison: 1 infantry slot; land only while occupied. Kit seats one L1 inside HQ; extra infantry is the Units strip. Assault: door 50 HP, then garrison, then the hut flips owner. Colony HQ capture/destroy defeats that player (Victory / Defeat banner; Exit still works). Bow / pike / L2 wait. See `tests/engine/combat.test.ts`, `tests/engine/capture.test.ts`, and `tests/engine/building.test.ts`.

---

## 7. Netcode (last)

Contract: [`net.md`](net.md). Hygiene + Lockstep + MemoryChannel are in. Do not write sockets until you are wiring `server/`.

**Why.** If 1–6 are on the action pipe, this is a gate + a MatchHost mailbox, not a rewrite.

**Do this.** Same Session on WebSocket. `server/` is the Node room. `World.tick()` stays dumb.

**Not this step.** Host `World` as referee, rollback, NAT punch, WebRTC, Steam, live speed sync.

**Bar.** 3 tabs against local Node, same checksums. CI bar is N Worlds on MemoryChannel. EC2 room list + spectate follows.

---

## Explicitly later

| Thing | Why later |
|---|---|
| More huts / civs | Options, not machine |
| Food / tools as caps | Buildings-as-gates; matcher must be per-player first |
| Water, ships, islands | Pathing + render, no new ownership model |
| Sound | Feel |
| Donkeys, geologist, thief, mana | Specialists on top of 3/5/6 |
| Barracks, bow / pike / L2 | Combat extras on top of L1 melee + T1 garrison + capture |
| Killing workers, gold morale, death ghost | Combat extras on top of L1 melee |
| Work-area panel, original control panel | UI after the verbs exist |
| Priorities / distribution screen | After per-player matcher hurts |
| Construction-finder AI | After the scripted second player is boring |
| Save as a snapshot | Replay *is* save if the log exists |

---

## Forgotten on purpose (still real, not pillars)

- **Events / messages** — “under attack”, “can’t flatten.” Add when combat or flatten makes eyeballing insufficient.
- **Determinism hygiene** — no `Map` iteration as a rule of law; stable sorts; no floats in checksum. Step 1 makes this testable.
- **Work-area / pause production** — building panel, after selection exists.
