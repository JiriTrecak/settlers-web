# UI

HTML/CSS overlay. Not Three.js.

`src/ui` is HUD + lobby screens. Closed widgets: Session subscribes. Folder map: [`src/ui/index.md`](../../src/ui/index.md).

## Now

- Fps + zoom
- Exit confirm
- Main menu + multiplayer lobby (one map: Grid)

## Refusals

- Drawing buttons in the WebGL scene.
- Reading `World` fields from click handlers.
